const config = {
    content: ['./src/**/*.{js,ts,jsx,tsx,html}'],
    theme: {
        extend: {
            colors: {
                modal: '#00000080',
                sub: 'red',
            },
        },
    },
    plugins: [],
};

export default config;
